
{
  "name": "media-stack-widget",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build"
  },
  "dependencies": {
    "axios": "^0.24.0",  // Or any HTTP library you're using to fetch data
    "react": "^17.0.0",   // For React-based projects
    "vite": "^2.6.0"      // If using Vite
  }
}
